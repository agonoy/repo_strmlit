name: My workflow

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      MY_SECRET: ${{ secrets.MY_API2 }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Run Python script
        run: python streamlit_app.py
